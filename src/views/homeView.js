import React from 'react';
import Landing from '../components/landing';
import {splash} from '../resources';

const HomeView = () => {
    return (
        <Landing splash={splash} />
    );
}

export default HomeView;
